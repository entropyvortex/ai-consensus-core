node_modules
dist
coverage
*.tsbuildinfo
*.tgz
.DS_Store
.env
.env.local
.env.*.local
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea

# Library — do not commit the lockfile; platform-specific optional deps
# (e.g. @rolldown/binding-*) break cross-platform CI when the lockfile
# is generated on a different arch than the runner. See npm/cli#4828.
package-lock.json
